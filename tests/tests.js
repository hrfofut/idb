import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme'

import { FoodDetail } from '../idb/static/js/fooddetail.js';

describe('FoodDetail', function() {
  it('should have props for name, servings, calorie, sodium, fat, and protein', function () {
    
  });

  it('should have a title', function () {

  });

  it('should render a table', function () {

  });

  it('test' function() {
    assert.equal(1, 1);
  });
});
